# Shadow identity configuration (authoritative at live boot)
# Overrides the default in the common kali.conf (loaded before this file).
LIVE_HOSTNAME="egypt"
LIVE_USERNAME="shadow"
LIVE_USER_FULLNAME="Shadow"